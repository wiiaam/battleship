package game.ship;

/**
 *  Rotation is relative to the start point, facing down.
 *  If the rotation of the ship is 0, then the start point is at the top of the ship.
 */
public enum Rotation {
    R0, R90, R180, R270
}
